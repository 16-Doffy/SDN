//const express = require("express");
// const app = express();
// const hostname = "localhost";
// const port = 5000;

// // Resource không có tham số
// app.get("/product", (req, res) => {
//   res.status(200).set("Content-Type", "text/plain").send("All products");
// });

// // Resource có tham số
// app.get("/product/:id", (req, res) => {
//   const id = req.params.id;
//   res.status(200).set("Content-Type", "text/plain").send(`Product with id: ${id}`);
// });

// // Default response cho các route khác
// app.use((req, res) => {
//   res.status(404).set("Content-Type", "text/plain").send("Not found");
// });

// app.listen(port, hostname, () => {
//   console.log("server is running");
// });
